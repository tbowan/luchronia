
#------------------------------------------------------------------------------
# Phase 1 : level points
#------------------------------------------------------------------------------

update game_skill_character
  set learning = 100 * (1 + `level` * (`level` + 1) / 2)
  where `level` > 0 ;
